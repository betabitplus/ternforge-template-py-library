name: CI

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize, edited]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  ci:
    name: ci
    uses: betabitplus/ternforge-infra-ci/.github/workflows/python-library.yml@57415eaa90b5b0469d9ea75ad9020c69325f3af3 # v5.4.0
    with:
      python-version: "3.13"
      runtime-audit-exclude-package: "[[[ runtime_audit_exclude_package ]]]"
      runtime-audit-ignore-vulnerabilities: "[[[ pip_audit_waivers | map(attribute='id') | join(' ') ]]]"
      playwright-browsers: "[[[ ci_playwright_browsers | join(' ') ]]]"
    secrets:
      app-private-key: ${{ secrets.TERNFORGE_SOURCE_READ_PRIVATE_KEY }}
